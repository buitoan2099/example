import {ColorTheme} from 'src/commons/types';

export interface ColorType extends ColorTheme {
  primary: string;
  white: string;
  green: string;
  red: string;
  yellow: string;
  black: string;
  midnightBlue: string;
  darkGray: string;
  gray: string;
  lightGray: string;
  buttonNormal: string;
  buttonDisable: string;
}
